<Project Sdk="Transpose.Build.Target/26.8.4125">
  <PropertyGroup>
    <TargetFramework>netstandard2.0</TargetFramework>
    <DefineConstants>TRANSPOSE</DefineConstants>
    <AssemblyName>app</AssemblyName>
    <RootNamespace>Tesserae.Monaco.Sample</RootNamespace>
    <LangVersion>7.2</LangVersion>
    <MinifyLocalVariables>true</MinifyLocalVariables>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Transpose.BCL" Version="26.9.4757" />
    <PackageReference Include="Transpose.Core" Version="26.9.4756" />
    <PackageReference Include="Tesserae" Version="2026.9.70279" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Tesserae.Monaco\Tesserae.Monaco.csproj" />
  </ItemGroup>

  <!--
    A PackageReference would get this automatically (it lives in the package's buildTransitive/), but
    a ProjectReference does not import the referenced project's targets - so the sample imports it by
    hand. This is the same file a real consumer runs, which is the point: the sample exercises the
    actual Monaco asset copy rather than a bespoke one.
  -->
  <Import Project="..\Tesserae.Monaco\buildTransitive\Tesserae.Monaco.targets" />
</Project>
